---
import Layout from "@layouts/Layout.astro";
import Footer from "@components/Footer.astro";
import Counter from "@components/Counter.astro";
import counter from "@content/counter.json";
import { homepage } from "@content/homepage";
import Features from "@components/Features.astro";
import { Icon } from "astro-icon";
---

<Layout>
  <section class="grid py-10 place-items-center text-center">
    <h1 class="text-4xl mb-5">Simeng (Simon) He</h1>
    <p class="">
      Senior Computer science student at the University of Waterloo! Currently seeking Software Engineering Roles (New Grad) +.
    </p>
    <p class="">
      Previously interned <a href="https://noom.com">@noom</a> and <a
        href="https://tesla.com">@tesla</a
      >.
      <br />
      Software Engineering Intern on Localization & Release team <a
        target="_blank"
        href="https://apple.com">@apple</a
      >
    </p>
    <div>
      <a target="_blank" href="https://www.linkedin.com/in/simeng-he/">
        <Icon
          name="uil:linkedin"
          class="overflow-hidden inline-flex sm:m-8 w-28 h-28"
        />
      </a>
    </div>
  </section>
  <!-- {
    homepage.map((homepage) => (
      <Features
        title={homepage.title}
        icon={homepage.icon}
        content={homepage.content}
      />
    ))
  } -->
  <!-- <section
    class="py-20 sm:flex sm:items-center sm:justify-evenly text-center sm:text-left"
  >
    {
      (
        <ul class="grid sm:grid-cols-3 gap-16">
          {counter.map((counter) => (
            <Counter number={counter.number} label={counter.label} />
          ))}
        </ul>
      )
    }
  </section> -->
  <Footer />
</Layout>
